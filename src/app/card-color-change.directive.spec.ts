import { CardColorChangeDirective } from './card-color-change.directive';

describe('CardColorChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new CardColorChangeDirective();
    expect(directive).toBeTruthy();
  });
});
